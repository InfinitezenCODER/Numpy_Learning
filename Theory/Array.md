Numpy is used to store all sort of datas 1D array , 2D array , 3D array ..
We use Numpy over list because numpy is very fast rather than Lists due to the spaces that takes and contiguous memory .
list :
a = [1,2,3]
b = [2,4,6]
Mathematical  expressions will give error.
Numpy :
a =np.array([1,2,3])
b =np.array([2,4,6])
a * b = [2,8,18]
